// import { useLoaderData } from 'react-router-dom';

function PostDetailPage() {
  return (
    <>
      <p>post details</p>
    </>
  );
}

export default PostDetailPage;

// page loaded after data fetching
// export function loader({ params }) {
//   const postId = params.id;

//   return getPost(postId);
// }
